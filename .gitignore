# Render leftovers from producing the social image with headless Chrome.
wrap*.html
chrome-profile/
cp/

# The kit-version stamp upgrade.py writes next to the tools it just installed. It belongs to
# an installed copy, not to the sources this repo builds from.
tools/kit-version.txt

# Local experiments and cold-run scratch. Nothing here is part of the kit.
_scratch/
*.tmp

.DS_Store
Thumbs.db

# Python bytecode from running the shipped tools/suites.
__pycache__/
*.pyc

# Marketing drafts live outside the repo: ../vault-kit-promo
promo/
